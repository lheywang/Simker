services:
  simker:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/workspace
      - /tmp/.X11-unix:/tmp/.X11-unix
    environment:
      - DISPLAY=${DISPLAY}
      - TZ=Europe/Paris
      - TERM=xterm-256color
    working_dir: /workspace
    user: designer
